# 🚀 Netlify自动化部署指南

## ⚠️ 重要说明

由于项目部署在Netlify而非Vercel，我们使用**GitHub Actions**来实现定时任务，而不是`vercel.json`。

## 🤖 自动化方案

### 1. GitHub Actions定时任务

**配置文件**: `.github/workflows/daily-news-analysis.yml`

**执行时间**: 每天22:00北京时间（14:00 UTC）

**工作流程**:
1. GitHub Actions定时触发
2. 调用Netlify部署的API端点 `/api/cron/daily-update`
3. API执行新闻抓取和AI分析
4. 数据自动保存到本地存储

### 2. 手动触发方式

#### 方式1: GitHub Actions手动触发
1. 访问GitHub仓库
2. 点击 **Actions** 选项卡
3. 选择 **Daily News Analysis** 工作流
4. 点击 **Run workflow** 按钮

#### 方式2: 直接调用API
```bash
curl -X POST https://xinwenlianbo.netlify.app/api/cron/daily-update
```

#### 方式3: 浏览器访问
```
https://xinwenlianbo.netlify.app/api/cron/daily-update
```

## 📅 定时任务配置

### 当前配置
- **时间**: 每天14:00 UTC (22:00北京时间)
- **频率**: 每日执行
- **触发器**: GitHub Actions Cron

### 修改执行时间
编辑 `.github/workflows/daily-news-analysis.yml` 中的 cron 表达式:

```yaml
schedule:
  - cron: '0 14 * * *'  # 22:00北京时间
  - cron: '0 13 * * *'  # 21:00北京时间
  - cron: '30 14 * * *' # 22:30北京时间
```

## 🔧 环境要求

### GitHub Secrets (可选)
如果需要额外的安全验证，可以在GitHub仓库设置中添加:
- `CRON_SECRET`: API调用密钥

### Netlify环境变量
确保以下环境变量已配置:
- `QWEN_API_KEY`: 千问API密钥

## 📊 监控和日志

### 查看执行状态
1. **GitHub Actions**: 查看工作流执行日志
2. **Netlify Functions**: 查看函数执行日志
3. **API响应**: 直接调用API查看返回结果

### 成功响应示例
```json
{
  "success": true,
  "message": "每日更新任务完成",
  "date": "2025-11-30",
  "newsCount": 12,
  "analysisCount": 12,
  "statistics": {
    "industries": 25,
    "companies": 18,
    "futures": 8,
    "bonds": 10,
    "sentiment": {
      "bullish": 7,
      "bearish": 2,
      "neutral": 3
    }
  },
  "timestamp": "2025-11-30T14:00:00.000Z"
}
```

## ✅ 优势

### GitHub Actions方案优势
1. **免费**: GitHub Actions对公共仓库免费
2. **可靠**: GitHub的基础设施稳定
3. **灵活**: 支持手动触发和定时执行
4. **监控**: 完整的执行日志和状态
5. **兼容**: 与Netlify完美配合

### 与Vercel Cron对比
| 特性 | GitHub Actions | Vercel Cron |
|------|----------------|--------------|
| 平台兼容 | ✅ Netlify | ❌ 仅Vercel |
| 免费额度 | ✅ 充足 | ✅ 有限 |
| 手动触发 | ✅ 支持 | ✅ 支持 |
| 监控日志 | ✅ 详细 | ✅ 详细 |
| 配置复杂度 | 🟡 中等 | 🟢 简单 |

## 🎯 总结

使用GitHub Actions + Netlify API的方案完全可以替代Vercel Cron，并且更加灵活可靠。

**明天和后天的自动化运行完全有保障！** 🚀
